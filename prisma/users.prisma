model users {
  id          String   @id @default(uuid())
  name        String
  email       String   @unique
  password    String
  age         Int?
  isVerified  Boolean  @default(false)
  otp         String?
  createdAt   DateTime @default(now())
}